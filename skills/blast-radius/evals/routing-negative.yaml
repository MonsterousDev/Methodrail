id: skill.blast-radius.routing-negative
kind: routing
skill: blast-radius
description: "Mechanical rename does not require blast-radius."
input:
  prompt: "Rename getUserById to findUserById. Formatting only."
expected:
  skills:
    forbidden: [blast-radius]
