Completed TASK.md.

Password-reset mail now queues a `password-reset` template through `enqueueMail`, matching the welcome-mail path. Tests were updated and `npm test` passes with 2 tests.

Wrote the required artifacts:
- `ANSWER.md`
- `COMMANDS.json`