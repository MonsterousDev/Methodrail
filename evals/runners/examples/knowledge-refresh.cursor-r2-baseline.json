{
  "fixture_id": "knowledge-refresh",
  "condition": "baseline",
  "host": "cursor",
  "model": "grok-4.6",
  "repeat": 2,
  "skills_invoked": [],
  "references_loaded": [],
  "tools_used": [
    "edit",
    "shell"
  ],
  "subagents_used": 0,
  "verification_steps": [
    {
      "command": "node --test",
      "exit_status": 0
    }
  ],
  "evidence": [
    "# Answer",
    "## What changed",
    "`ticket.resolved` customer emails now go through the same outbox path as `ticket.opened`.",
    "- `repo/src/mailer.js`: `handle()` dispatches `ticket.resolved` with `dispatchOnce(event.id, event.type, event.data)`, so the event is mailed once per id and counted on the `ticket.resolved` channel.",
    "- `repo/src/mailer.test.js`: replaced the pending “not routed yet” assertion with a routing + idempotency test (handle twice, one delivery).",
    "## Why",
    "The mailer already treated provider events as outbox rows keyed by event id. `ticket.resolved` was the missing event type: `handle()` returned `null` and sent nothing. Routing it through `dispatchOnce` reuses duplicate protection and failure handling (`failSend` / `markFailed`) without a second send path.",
    "## Tests",
    "`node --test` in `repo/` — 3 passed."
  ],
  "outcome": "# Answer ## What changed `ticket.resolved` customer emails now go through the same outbox path as `ticket.opened`. - `repo/src/mailer.js`: `handle()` dispatches `ticket.resolved` with `dispatchOnce(event.id, event.type, event.data)`, so the event is mailed once per id and counted on the `ticket.resolved` channel. - `repo/src/mailer.test.js`: replaced the pending “not routed yet” assertion with a routing + idempotency test (handle twice, one delivery). ## Why The mailer already treated provider e",
  "failure_modes": [],
  "notes": "Live v0.7 pilot 2026-08-25. Worktree /tmp/methodrail-pilot-20260825-v07/knowledge-refresh/cursor-r2-baseline. Cursor runs used isolated Task subagents on /tmp copies. Codex used codex exec --ignore-user-config --skip-git-repo-check --sandbox workspace-write -m gpt-5.5. Extra copy; canonical example JSON was not replaced.",
  "provenance": "live",
  "capture": "operator_summary",
  "artifacts": {
    "overlay": "evals/runners/artifacts/knowledge-refresh/cursor-r2-baseline/overlay",
    "command_log": "evals/runners/artifacts/knowledge-refresh/cursor-r2-baseline/command.log.json",
    "answer": "evals/runners/artifacts/knowledge-refresh/cursor-r2-baseline/answer.md"
  }
}
