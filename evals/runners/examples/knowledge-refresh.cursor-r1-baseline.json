{
  "fixture_id": "knowledge-refresh",
  "condition": "baseline",
  "host": "cursor",
  "model": "grok-4.6",
  "repeat": 1,
  "skills_invoked": [],
  "references_loaded": [],
  "tools_used": [
    "edit",
    "shell"
  ],
  "subagents_used": 0,
  "verification_steps": [
    {
      "command": "node --test",
      "exit_status": 0
    }
  ],
  "evidence": [
    "# Answer",
    "## What changed",
    "`handle()` in `repo/src/mailer.js` now routes `ticket.resolved` the same way as `ticket.opened`: through `dispatchOnce` with the event id as the idempotency key, the event type as the channel, and `event.data` as the body.",
    "The pending-route test in `repo/src/mailer.test.js` was replaced with a duplicate-delivery check so `ticket.resolved` is asserted as a first-class mail path.",
    "## Why",
    "The mailer already had an outbox that dedupes by event id and records channel/body/status. Resolved-ticket customer emails only needed that same dispatch path; they do not need a separate send API. Using `dispatchOnce` keeps retries and failure marking (`failSend` / `markFailed`) working for resolved events without extra state."
  ],
  "outcome": "# Answer ## What changed `handle()` in `repo/src/mailer.js` now routes `ticket.resolved` the same way as `ticket.opened`: through `dispatchOnce` with the event id as the idempotency key, the event type as the channel, and `event.data` as the body. The pending-route test in `repo/src/mailer.test.js` was replaced with a duplicate-delivery check so `ticket.resolved` is asserted as a first-class mail path. ## Why The mailer already had an outbox that dedupes by event id and records channel/body/stat",
  "failure_modes": [],
  "notes": "Live v0.7 pilot 2026-08-25. Worktree /tmp/methodrail-pilot-20260825-v07/knowledge-refresh/cursor-r1-baseline. Cursor runs used isolated Task subagents on /tmp copies. Codex used codex exec --ignore-user-config --skip-git-repo-check --sandbox workspace-write -m gpt-5.5. Extra copy; canonical example JSON was not replaced.",
  "provenance": "live",
  "capture": "operator_summary",
  "artifacts": {
    "overlay": "evals/runners/artifacts/knowledge-refresh/cursor-r1-baseline/overlay",
    "command_log": "evals/runners/artifacts/knowledge-refresh/cursor-r1-baseline/command.log.json",
    "answer": "evals/runners/artifacts/knowledge-refresh/cursor-r1-baseline/answer.md"
  }
}
