{
  "fixture_id": "decision-ladder",
  "condition": "methodrail",
  "host": "cursor",
  "model": "grok-4.6",
  "repeat": 1,
  "skills_invoked": [],
  "references_loaded": [],
  "tools_used": [
    "edit",
    "shell"
  ],
  "subagents_used": 0,
  "verification_steps": [
    {
      "command": "bash skills/show-me-your-work/scripts/log.sh decisions.tsv persist \"log each write as JSON not text\" \"reversible parseable trail; not hard to reverse\" \"file:src/store.js\" \"open\"",
      "exit_status": 0
    },
    {
      "command": "bash skills/show-me-your-work/scripts/log.sh decisions.tsv persist \"store orders in a JSON file not SQLite\" \"no db dependency, single-process persist API, proportional for current risk\" \"file:src/store.js\" \"open\"",
      "exit_status": 0
    },
    {
      "command": "bash skills/show-me-your-work/scripts/log.sh decisions.tsv persist \"promoted JSON file store choice to ADR-0002\" \"hard to reverse, surprising without context, real trade-off; operator approved after classification\" \"file:docs/adr/0002-file-store.md\" \"accepted\"",
      "exit_status": 0
    },
    {
      "command": "node --input-type=module -e '\nimport { persist, logWrite } from \"./src/store.js\";\nimport { readFileSync } from \"node:fs\";\n\nconst r1 = persist({ id: \"ord-1\", total: 12 });\nif (!r1.ok || r1.id !== \"ord-1\") throw new Error(\"persist return mismatch\");\nconst r2 = persist({ id: \"ord-2\", total: 30 });\nconst logged = logWrite({ ts: \"2026-09-01T00:00:00.000Z\", action: \"manual\", id: \"probe\" });\nif (logged !== JSON.stringify({ ts: \"2026-09-01T00:00:00.000Z\", action: \"manual\", id: \"probe\" })) {\n  throw new Error(\"logWrite did not return JSON\");\n}\ntry {\n  persist({});\n  throw new Error(\"missing id should throw\");\n} catch (err) {\n  if (err.message !== \"order.id required\") throw err;\n}\nconst orders = JSON.parse(readFileSync(\"data/orders.json\", \"utf8\"));\nif (!orders[\"ord-1\"] || orders[\"ord-1\"].total !== 12) throw new Error(\"ord-1 missing\");\nif (!orders[\"ord-2\"] || orders[\"ord-2\"].total !== 30) throw new Error(\"ord-2 missing\");\nconst lines = readFileSync(\"data/writes.jsonl\", \"utf8\").trim().split(\"\\n\");\nif (lines.length < 3) throw new Error(\"expected at least 3 log lines\");\nfor (const line of lines) JSON.parse(line);\nconsole.log(JSON.stringify({ persist: r1, orders, writeLines: lines.length }, null, 2));\n'",
      "exit_status": 0
    },
    {
      "command": "bash skills/show-me-your-work/scripts/log.sh decisions.tsv persist \"verified JSON file persist and JSON write log\" \"node persist round-trip wrote data/orders.json and data/writes.jsonl\" \"file:data/orders.json\" \"tests green\"",
      "exit_status": 0
    },
    {
      "command": "test -f ANSWER.md && test -f COMMANDS.json && python3 -c 'import json; json.load(open(\"COMMANDS.json\")); print(\"ANSWER.md and COMMANDS.json exist; COMMANDS.json is valid JSON\")'",
      "exit_status": 0
    }
  ],
  "evidence": [
    "Order persistence is in place. `persist(order)` writes to a JSON file store at `data/orders.json` (keyed by id) and logs each write as one JSON line in `data/writes.jsonl`. `logWrite` returns that JSON line. Missing `order.id` still throws.",
    "Two choices were classified before writing:",
    "1. **Write log format (JSON vs text)** — reversible, so TSV-only. Logged in `decisions.tsv`. No ADR.",
    "2. **File-backed store (JSON file vs SQLite)** — hard to reverse, surprising without context, and a real trade-off. After that classification, the battery operator’s store-choice approval applied. Durable record: `docs/adr/0002-file-store.md`. Indexed from `.methodrail/PROJECT.md`. Promotion also recorded in `decisions.tsv`. A later TSV row cannot override that ADR.",
    "JSON file over SQLite: this shop has no database dependency, a single-process persist API, and no stated concurrency or query needs. SQLite remains the path if concurrent writers become real.",
    "**Observed (this run):** `persist({ id: \"ord-1\", total: 12 })` and `persist({ id: \"ord-2\", total: 30 })` returned `{ ok: true, id }` and landed in `data/orders.json`. Three JSON lines were appended to `data/writes.jsonl`. `persist({})` threw `order.id required`. Independent review on a different model family was unavailable; this is a self-audit of the trail against the files written."
  ],
  "outcome": "Order persistence is in place. `persist(order)` writes to a JSON file store at `data/orders.json` (keyed by id) and logs each write as one JSON line in `data/writes.jsonl`. `logWrite` returns that JSON line. Missing `order.id` still throws. Two choices were classified before writing: 1. **Write log format (JSON vs text)** — reversible, so TSV-only. Logged in `decisions.tsv`. No ADR. 2. **File-backed store (JSON file vs SQLite)** — hard to reverse, surprising without context, and a real trade-off",
  "failure_modes": [],
  "notes": "Live v0.9 project-artifact-interoperability pilot 2026-09-01. Freeze HEAD 046342b4cfa012dc33ca5527ef680900bd3e2161 (uncommitted v0.9 graders). Worktree /tmp/methodrail-pilot-20260901-v09/decision-ladder/cursor-r1-methodrail. Cursor runs used isolated Task subagents on /tmp copies. Codex used codex exec --ignore-user-config --skip-git-repo-check --sandbox workspace-write --ignore-rules -m gpt-5.5. Extra copy; canonical example JSON was not replaced.",
  "provenance": "live",
  "capture": "operator_summary",
  "artifacts": {
    "overlay": "evals/runners/artifacts/decision-ladder/cursor-r1-methodrail/overlay",
    "command_log": "evals/runners/artifacts/decision-ladder/cursor-r1-methodrail/command.log.json",
    "answer": "evals/runners/artifacts/decision-ladder/cursor-r1-methodrail/answer.md"
  }
}
