{
  "fixture_id": "knowledge-refresh",
  "condition": "methodrail",
  "host": "cursor",
  "model": "grok-4.6",
  "repeat": 1,
  "skills_invoked": [],
  "references_loaded": [],
  "tools_used": [
    "edit",
    "shell"
  ],
  "subagents_used": 0,
  "verification_steps": [
    {
      "command": "git rev-parse --is-inside-work-tree 2>/dev/null; git log --oneline -20; echo '---'; git log --oneline cccccccccccccccccccccccccccccccccccccccc..HEAD -- repo/src/mailer.js repo/src/outbox.js 2>&1 | head -50; echo '---DIFF---'; git diff cccccccccccccccccccccccccccccccccccccccc -- repo/src/mailer.js repo/src/outbox.js 2>&1 | head -200; echo '---STATUS---'; git status --short",
      "exit_status": 128
    },
    {
      "command": "node --test",
      "exit_status": 0
    }
  ],
  "evidence": [
    "# Answer",
    "## What changed",
    "`ticket.resolved` customer emails now route the same way as `ticket.opened`: `handle` passes `event.id` into `dispatchOnce` so delivery is keyed on the provider event ID.",
    "- `repo/src/mailer.js` — route `ticket.resolved` through `dispatchOnce`.",
    "- `repo/src/mailer.test.js` — replaced the pending “not routed yet” check with duplicate-delivery and failed-send tests.",
    "`node --test` in `repo/`: 4 pass, 0 fail.",
    "## Project knowledge",
    "Used `.methodrail/PROJECT.md` → [mail](.methodrail/knowledge/mail.md). Followed [reuse](references/knowledge/reuse.md): read kind/status/evidence/`validated_at`, then checked against current source.",
    "There is no git repository here, so SHA `cccccccccccccccccccccccccccccccccccccccc` could not be resolved. Freshness is **unknown**. Current source was the source of truth.",
    "**Still valid:** customer ticket emails are keyed on the provider event ID (`ticket.opened` already passed `event.id` into `dispatchOnce`; `byKey` is keyed by that id).",
    "**Stale:** “a failed send deletes the outbox row so the next attempt is a clean insert.” Current `markFailed` keeps the row and sets `status: \"failed\"`. Tests assert that. `retryFailed` retries in place. Reuse guidance that said to delete-on-fail was not followed.",
    "The new `ticket.resolved` path uses the still-valid event-id key and keeps the failed outbox row, matching current source and tests. The stored note was not rewritten."
  ],
  "outcome": "# Answer ## What changed `ticket.resolved` customer emails now route the same way as `ticket.opened`: `handle` passes `event.id` into `dispatchOnce` so delivery is keyed on the provider event ID. - `repo/src/mailer.js` — route `ticket.resolved` through `dispatchOnce`. - `repo/src/mailer.test.js` — replaced the pending “not routed yet” check with duplicate-delivery and failed-send tests. `node --test` in `repo/`: 4 pass, 0 fail. ## Project knowledge Used `.methodrail/PROJECT.md` → [mail](.methodr",
  "failure_modes": [],
  "notes": "Live v0.7 pilot 2026-08-25. Worktree /tmp/methodrail-pilot-20260825-v07/knowledge-refresh/cursor-r1-methodrail. Cursor runs used isolated Task subagents on /tmp copies. Codex used codex exec --ignore-user-config --skip-git-repo-check --sandbox workspace-write -m gpt-5.5. Extra copy; canonical example JSON was not replaced.",
  "provenance": "live",
  "capture": "operator_summary",
  "artifacts": {
    "overlay": "evals/runners/artifacts/knowledge-refresh/cursor-r1-methodrail/overlay",
    "command_log": "evals/runners/artifacts/knowledge-refresh/cursor-r1-methodrail/command.log.json",
    "answer": "evals/runners/artifacts/knowledge-refresh/cursor-r1-methodrail/answer.md"
  }
}
