{
  "fixture_id": "init-value",
  "condition": "methodrail",
  "host": "cursor",
  "model": "grok-4.6",
  "repeat": 2,
  "skills_invoked": [],
  "references_loaded": [],
  "tools_used": [
    "shell"
  ],
  "subagents_used": 0,
  "verification_steps": [
    {
      "command": "npm install",
      "exit_status": 0,
      "phase": "other"
    },
    {
      "command": "npm test",
      "exit_status": 0,
      "phase": "other"
    }
  ],
  "evidence": [
    "# Init-value: hello-world example regression test",
    "Added an in-process regression test for the hello-world example. Library behavior (`lib/app.js`, `index.js`, `examples/hello.js`) is unchanged.",
    "## What changed",
    "- `repo/test/hello-example.test.js` (new): `require`s `examples/hello.js` and drives GET `/hello` with the same fake request helper as `test/hello.test.js`. Does not call `app.listen`.",
    "- `repo/package.json`: `npm test` now runs both `test/hello.test.js` and `test/hello-example.test.js`.",
    "## Install and verification",
    "CI in this repo uses `npm install` (`.npmrc` has `package-lock=false`; there is no lockfile). Observed:",
    "- `npm install` in `repo/` \u2192 exit 0"
  ],
  "outcome": "Added an in-process regression test for the hello-world example. Library behavior (`lib/app.js`, `index.js`, `examples/hello.js`) is unchanged. - `repo/test/hello-example.test.js` (new): `require`s `examples/hello.js` and drives GET `/hello` with the same fake request helper as `test/hello.test.js`. Does not call `app.listen`. - `repo/package.json`: `npm test` now runs both `test/hello.test.js` and `test/hello-example.test.js`. CI in this repo uses `npm install` (`.npmrc` has `package-lock=false",
  "failure_modes": [],
  "notes": "Live pilot 2026-08-25. Worktree /tmp/methodrail-pilot-20260825/init-value/cursor-r2-methodrail. Cursor runs used isolated Task subagents on /tmp copies of the fixture; they are still Cursor Grok in this product. Codex used `codex exec --ignore-user-config --skip-git-repo-check --sandbox workspace-write -m gpt-5.5`. Claude Code was not logged in; no Claude traces. Extra copy; canonical example JSON was not replaced.",
  "provenance": "live",
  "capture": "operator_summary",
  "artifacts": {
    "overlay": "evals/runners/artifacts/init-value/cursor-r2-methodrail/overlay",
    "command_log": "evals/runners/artifacts/init-value/cursor-r2-methodrail/command.log.json",
    "answer": "evals/runners/artifacts/init-value/cursor-r2-methodrail/answer.md"
  }
}
