{
  "fixture_id": "knowledge-applicability",
  "condition": "methodrail",
  "host": "cursor",
  "model": "grok-4.6",
  "repeat": 2,
  "skills_invoked": [],
  "references_loaded": [],
  "tools_used": [
    "edit",
    "shell"
  ],
  "subagents_used": 0,
  "verification_steps": [
    {
      "command": "git rev-parse --is-inside-work-tree && git log -1 --oneline && git rev-parse HEAD && git cat-file -t bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb 2>&1; echo \"---diff relevant---\"; git diff bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb -- repo/src/notifications/dispatch.js repo/src/outbox.js 2>&1; echo \"---log relevant---\"; git log --oneline bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb..HEAD -- repo/src/notifications/dispatch.js repo/src/outbox.js 2>&1; echo \"---status---\"; git status --short",
      "exit_status": 128
    },
    {
      "command": "npm test",
      "exit_status": 0
    }
  ],
  "evidence": [
    "Added `legacy.ping` to the legacy sender via `sendDirect`, matching `legacy.alert`.",
    "Disposition: **refuse** `.methodrail/knowledge/notifications.md` for this change. The note is an active verified invariant, but its scope includes `repo/src/notifications` and **excludes** `repo/src/notifications/legacy`. Exclusion wins, so the customer-notice claim (route through `dispatchOnce` with the provider event ID) does not apply to `repo/src/notifications/legacy/sender.js`. Freshness is **unknown** (no git history; `validated_at` SHA cannot be resolved). Current source still matches the note on the included path (`notice.created` uses `dispatchOnce`) and the existing legacy path (`legacy.alert` uses `sendDirect`, colliding ids both send).",
    "Tests: `npm test` in `repo/` — 3 passed (observed)."
  ],
  "outcome": "Added `legacy.ping` to the legacy sender via `sendDirect`, matching `legacy.alert`. Disposition: **refuse** `.methodrail/knowledge/notifications.md` for this change. The note is an active verified invariant, but its scope includes `repo/src/notifications` and **excludes** `repo/src/notifications/legacy`. Exclusion wins, so the customer-notice claim (route through `dispatchOnce` with the provider event ID) does not apply to `repo/src/notifications/legacy/sender.js`. Freshness is **unknown** (no g",
  "failure_modes": [],
  "notes": "Live v0.8 knowledge-governance pilot 2026-08-31. Freeze e22ff0ab9d7b0d245590edf29e592dd596693efc. Worktree /tmp/methodrail-pilot-20260831-v08/knowledge-applicability/cursor-r2-methodrail. Cursor runs used isolated Task subagents on /tmp copies. Codex used codex exec --ignore-user-config --skip-git-repo-check --sandbox workspace-write -m gpt-5.5. Extra copy; canonical example JSON was not replaced.",
  "provenance": "live",
  "capture": "operator_summary",
  "artifacts": {
    "overlay": "evals/runners/artifacts/knowledge-applicability/cursor-r2-methodrail/overlay",
    "command_log": "evals/runners/artifacts/knowledge-applicability/cursor-r2-methodrail/command.log.json",
    "answer": "evals/runners/artifacts/knowledge-applicability/cursor-r2-methodrail/answer.md"
  }
}
