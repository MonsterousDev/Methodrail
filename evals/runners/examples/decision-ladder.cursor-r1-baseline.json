{
  "fixture_id": "decision-ladder",
  "condition": "baseline",
  "host": "cursor",
  "model": "grok-4.6",
  "repeat": 1,
  "skills_invoked": [],
  "references_loaded": [],
  "tools_used": [
    "edit",
    "shell"
  ],
  "subagents_used": 0,
  "verification_steps": [
    {
      "command": "node --input-type=module -e \"\nimport { persist, logWrite } from './src/store.js';\nconst a = persist({ id: 'ord-1', total: 12 });\nconst b = persist({ id: 'ord-1', total: 15 });\nconst c = persist({ id: 'ord-2', sku: 'tea' });\nlet threw = false;\ntry { persist({}); } catch (e) { threw = e.message === 'order.id required'; }\nconst extra = logWrite({ event: 'manual', note: 'ok' });\nconsole.log(JSON.stringify({ a, b, c, threw, extra }));\n\"",
      "exit_status": 0
    },
    {
      "command": "cat data/orders.json && echo '---' && cat data/writes.jsonl",
      "exit_status": 0
    },
    {
      "command": "rm -rf data",
      "exit_status": 0
    }
  ],
  "evidence": [
    "Order persistence is in place.",
    "`persist(order)` writes orders to `data/orders.json` (JSON array, upsert by `id`). Missing `order.id` still throws. Each persist also appends one JSON object to `data/writes.jsonl`. `logWrite(row)` is the same JSON-lines logger and returns the line it wrote.",
    "Store choice: JSON file, not SQLite. This shop has no native dependencies; a JSON file is inspectable and enough at current scale. SQLite would add a module, a schema, and a later-surprising migration. That decision is in `docs/adr/0002-file-store.md` and `decisions.tsv`.",
    "Log format: JSON (one object per line), not free-form text. Recorded in the same ADR and TSV. Data files are created on first write."
  ],
  "outcome": "Order persistence is in place. `persist(order)` writes orders to `data/orders.json` (JSON array, upsert by `id`). Missing `order.id` still throws. Each persist also appends one JSON object to `data/writes.jsonl`. `logWrite(row)` is the same JSON-lines logger and returns the line it wrote. Store choice: JSON file, not SQLite. This shop has no native dependencies; a JSON file is inspectable and enough at current scale. SQLite would add a module, a schema, and a later-surprising migration. That dec",
  "failure_modes": [],
  "notes": "Live v0.9 project-artifact-interoperability pilot 2026-09-01. Freeze HEAD 046342b4cfa012dc33ca5527ef680900bd3e2161 (uncommitted v0.9 graders). Worktree /tmp/methodrail-pilot-20260901-v09/decision-ladder/cursor-r1-baseline. Cursor runs used isolated Task subagents on /tmp copies. Codex used codex exec --ignore-user-config --skip-git-repo-check --sandbox workspace-write --ignore-rules -m gpt-5.5. Extra copy; canonical example JSON was not replaced.",
  "provenance": "live",
  "capture": "operator_summary",
  "artifacts": {
    "overlay": "evals/runners/artifacts/decision-ladder/cursor-r1-baseline/overlay",
    "command_log": "evals/runners/artifacts/decision-ladder/cursor-r1-baseline/command.log.json",
    "answer": "evals/runners/artifacts/decision-ladder/cursor-r1-baseline/answer.md"
  }
}
