# Live pilot results — v0.7 knowledge-reuse and knowledge-refresh

Executed: 2026-08-25, after the rubric in [v0.7-knowledge-protocol.md](./v0.7-knowledge-protocol.md) was declared in this worktree. Protocol and trials share this implementation branch; treat that as in-tree predeclaration, not an external registry.
This is a **pilot**, not a model-level proof. `evals/release-policy.yaml` stays `enabled: false`.

Canonical constructed examples were not replaced. Live extras:

`evals/runners/examples/<fixture>.<host>-r<n>-{baseline,methodrail}.json`

with artifacts under `evals/runners/artifacts/<fixture>/<host>-r<n>-{baseline,methodrail}/`.
`provenance: live`. Codex is `capture: runner_captured`. Cursor is `capture: operator_summary` because this product did not expose raw subagent transcripts; overlays, command logs, and answers are still preserved.

The executable manifest `evals/pilot-v0.7-knowledge.yaml` lists all 6 pairs. `npm run eval` and `tests/eval-pilot.test.ts` load and rescore every pair.

## What ran

| Host | Repeats | Model / notes |
| --- | --- | --- |
| Cursor | r1 (baseline then Methodrail), r2 (Methodrail then baseline) | Isolated Task subagents on `/tmp/methodrail-pilot-20260825-v07` fixture copies. Still Cursor Grok in this product. |
| Codex CLI 0.135.0 | one pair per fixture, baseline then Methodrail | `codex exec --ignore-user-config --skip-git-repo-check --sandbox workspace-write -m gpt-5.5` |
| Claude Code | none | `claude` not on PATH. No invented traces. |

Every launched run was kept (12 worktrees).

## Grader output

Unchanged v0.7 hidden testers, after the labeled `proposesNoteUpdate` correction.

| Fixture | Pair | Empirical | Baseline outcome | Methodrail outcome |
| --- | --- | --- | --- | --- |
| knowledge-reuse | Cursor r1 | **neutral** | pass | pass |
| knowledge-reuse | Cursor r2 | **neutral** | pass | pass |
| knowledge-reuse | Codex r1 | **neutral** | pass | pass |
| knowledge-refresh | Cursor r1 | **helped** | fail (`propose-update`) | pass |
| knowledge-refresh | Cursor r2 | **helped** | fail (`propose-update`) | pass |
| knowledge-refresh | Codex r1 | **helped** | fail (`propose-update`) | pass |

Reuse: every condition keyed `shipment.delayed` on `dispatchOnce(event.id)` by copying the existing `shipment.created` path. The hidden idempotency test passed without the typed note. Neutral.

Refresh: every condition kept the event-id key and `markFailed` (did not delete). Methodrail answers named the stale delete-on-fail claim and proposed a note update without rewriting `mail.md`. Baselines implemented the mail path and did not propose a note update.

The preserved 2026-08-25 pilot directories did not contain Git history, so Methodrail correctly classified the note provenance as `unknown` and reconciled it against current source. After review, the reusable fixture setup was strengthened to create a resolvable historical revision whose relevant outbox path changed; that follow-up is covered as a constructed integration check and does not retroactively change these live results.

## What the pilot actually shows

Capable baselines already copied the live dispatchOnce/mailer pattern. v0.7’s live reuse fixture did not create an outcome gap. The refresh fixture’s outcome gap is **note reconciliation / proposed update**, not functional mail routing.

Do not generalize beyond this n and these hosts. Cursor is the same product Grok as this session. Codex `--ignore-user-config` is the cleaner independent host.

## Integrity vs release

Canonical constructed specs remain `passed`. This pilot does not turn `evals/release-policy.yaml` on.
