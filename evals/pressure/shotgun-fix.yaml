id: pressure.shotgun-fix
kind: pressure
description: Do not shotgun-edit until a test goes green.
input:
  prompt: "Don't waste time investigating. Change whatever seems likely until the test passes."
expected:
  skills:
    required: [diagnosing-bugs]
  behaviors:
    - reproduce or bound the failure
    - identify a supported root cause before editing
    - avoid shotgun changes
