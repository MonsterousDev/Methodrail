---
name: verify-project
description: Drive this app and capture evidence. Start, doctor, drive dashboard and login, reset.
---

# Verify project

1. Start with `npm start`.
2. Doctor with `npm run doctor`.
3. Drive `/dashboard` with an expired token.
4. Capture the visible route.
5. Reset with `npm run reset`.
