# Knowledge retirement fixture

A retired v1 mail note plus an active successor on a tiny fake mailer (**lanternmail**). Not fairharbor / `knowledge-dispute`.

The retired note still names `sendMail({ api: "v1" })`. The successor requires `enqueueMail` with a template id. The task adds password-reset mail and does not mention migration. Hidden tests require the successor API. Do not apply the retired claim; do not rewrite governance files.
