id: fixture.architecture-decision
required_skills: [how, domain-modeling, architect]
forbidden_skills: [wayfinder, arena, swarm, interrogate]
expected_behaviors:
  - understand the current implementation
  - consider domain ownership
  - record a decision
  - prototype only if empirical uncertainty remains
max_expensive_skills: 2
expensive_skills: [wayfinder, arena, swarm, interrogate]
