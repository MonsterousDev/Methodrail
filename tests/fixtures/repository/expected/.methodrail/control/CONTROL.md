# Control

Use the repository's existing commands:

- Start/exercise: `npm start -- Ada`
- Readiness: the process exits successfully and stdout is `Hello, Ada!`
- Inspect/capture: retain stdout, stderr, and exit status
- Check: `npm run check`
- Reset: no persistent state exists
- Stop: the CLI exits after one invocation

No generated runtime wrapper is required.
