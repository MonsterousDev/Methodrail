# Methodrail v0.6 init dogfood

Internal maintainer record. Operator: Grok Bot 2. Date: 2026-08-24. No git commit or push. Methodrail package files were not modified except this report.

## Target

| Field | Value |
| --- | --- |
| Repo | [expressjs/express](https://github.com/expressjs/express) |
| Clone | `/tmp/express-methodrail-dogfood` (shallow, `--depth 1`) |
| HEAD identity | `package.json` version **5.2.1**; Node engine `>= 18` |
| Project type (from evidence) | CommonJS HTTP **library/framework**, not a packaged long-running service |
| Why this target | Real, well-known Node HTTP surface with mocha + supertest tests and example servers. Not Methodrail. |

Inspected before writing: `Readme.md`, `package.json`, `.npmrc`, `.gitignore`, `.eslintrc.yml`, `.github/workflows/ci.yml` + `legacy.yml`, `index.js`, `lib/express.js`, `lib/application.js`, `test/app.js`, `test/support/env.js`, `test/acceptance/hello-world.js`, `examples/README.md`, `examples/hello-world/index.js`, `History.md`. Searched for `AGENTS.md`, `CLAUDE.md`, `.cursor/`, `.claude/`, `.codex/`, `.agents/`, `.github/copilot-instructions.md`, `.methodrail/` — all absent.

No questions were asked; the repository answered type, commands, and integration choice.

## PROJECT.md quality (`src/project-md.ts`)

`evaluateProjectMd` on the generated file:

| Heuristic | Limit | Observed | Result |
| --- | --- | --- | --- |
| Lines | ≤ 120 | **74** (`split(/\r?\n/)`; `wc -l` = 73) | pass |
| Characters | ≤ 8000 | **5005** | pass |
| Markdown link | ≥ 1 | many (`[text](url)` present) | pass |
| Code fence markers | ≤ 4 | **0** | pass |
| `ok` | true | true | pass |

Relative links from `.methodrail/PROJECT.md` all resolve on disk. External URLs returned HTTP 200: `https://expressjs.com/`, v5 migration guide, security policy, org `CONTRIBUTING.md`, `express-generator`.

## Checklist scorecard

Scoring: **pass** / **fail** / **n/a**. Evidence is from the Express clone and this procedure, not from Methodrail's own harness.

| # | Checklist item | Score | Evidence |
| --- | --- | --- | --- |
| 1 | Repository and existing AI guidance were inspected before questions. | **pass** | Interview order followed. No AI files existed. No human questions. |
| 2 | Project type, boundaries, and commands came from evidence. | **pass** | Type from `package.json` description + `lib/express.js` `createApplication`. Published surface from `"files": ["LICENSE","Readme.md","index.js","lib/"]`. Commands copied from `scripts` and CI (`npm install --ignore-scripts --include=dev`, `npm run lint`, `npm run test-ci`). `.npmrc` (`ignore-scripts=true`, `package-lock=false`) and gitignored lockfile → `npm install`, not `npm ci`. |
| 3 | `.methodrail/PROJECT.md` is a useful index, not a dump of the tree. | **pass** | 73/74 lines. No directory listing. Points at `lib/express.js`, `lib/application.js`, tests, examples README, CI. |
| 4 | PROJECT.md is pointer-oriented: links, small durable facts, and paths to verification/control/runtime docs. | **pass** | Durable facts: Node ≥ 18, published files, external `router`/`body-parser`, default settings. Verification is a command map. Control omitted with a pointer-reason, not a dumped procedure. |
| 5 | PROJECT.md is not a cloned README and not a generated repository summary. | **pass** | Omits philosophy, badges, TC/triager lists, generator walkthrough. Adds agent-only facts the Readme does not stress: `.npmrc`, no `npm ci`, no `lib/router.js`, tests must not `app.listen`, ESM snippet vs CJS source. |
| 6 | Optional knowledge, control, and project-local skills have a stated recurring use. | **n/a** | None created. Skip reason is in PROJECT.md Verification + Runtime: mocha/supertest already drives HTTP; start/doctor/drive/stop collapse to package scripts. No recurring uncertainty that would pay for `knowledge/` or `CONTROL.md`. |
| 7 | Control/verification questions were investigated when the project has a runnable surface. | **pass** | Start: examples listen only when `!module.parent` (`examples/hello-world/index.js`). Ready: `npm test`. Drive: supertest against the app function (`test/app.js`, `test/acceptance/hello-world.js`). Inspect/capture: mocha output. Reset/stop: in-process tests; example servers would be the child process, not a named killer. No CONTROL.md because those answers are one script each. |
| 8 | `create-verification-skill` ran when the surface is meaningful; static verification was documented when it is not. | **pass** | Surface is a **library**. Example `app.listen(3000)` is a demo, not a product to doctor. Skill was **not** invoked. Static map documented: `npm test` / `npm run lint` / `npm run test-ci` / acceptance tests for examples. |
| 9 | Global Methodrail skills were not copied into the project. | **pass** | Clone has no `SKILL.md`, no `how`/`tdd`/`wayfinder`. Only `.methodrail/PROJECT.md` and `AGENTS.md`. |
| 10 | Existing instructions and curated content are preserved. | **pass** | Nothing to preserve. `git status` in the clone is only `?? .methodrail/` and `?? AGENTS.md`. Readme, tests, lib untouched. |
| 11 | Exactly one supported integration is installed or an existing one is retained. | **pass** | Created **AGENTS.md** only, with the Methodrail-owned pointer block from `merge-semantics.md`. No `.cursor/rules`, no `CLAUDE.md`, no Copilot file. Choice: tool-neutral OSS library with no existing AI surface; AGENTS.md is the clearest convention. Cursor was not imposed despite this operator running in Cursor. |
| 12 | Refresh distinguished documentation drift from product regression. | **n/a** | First init. No prior `CONTROL.md` or generated knowledge to validate. |
| 13 | Monorepo detail is proportional to meaningful differences. | **n/a** | Single package. `examples/` is illustration + acceptance tests, not a workspace. |
| 14 | Paths and links resolve. | **pass** | All `../…` targets exist. External URLs HTTP 200 (see quality section). |
| 15 | Any skill frontmatter is valid and its name matches its folder. | **n/a** | No project-local skill. |
| 16 | Commands are copied accurately; risky commands were not executed merely to validate spelling. | **pass** | Scripts quoted from `package.json` / `ci.yml`. Did **not** run `npm install`, `npm test`, or example servers to “check spelling.” |
| 17 | An unchanged second run would produce no diff. | **pass** | See idempotency below. |
| 18 | Completion reports created, updated, preserved, skipped, refreshed, and unresolved items in concise bullets. | **pass** | This section. |

**Totals:** 14 pass, 0 fail, 4 n/a.

## Idempotency (mental second run)

Unchanged inputs: same clone, same evidence, no new user decisions.

- `.methodrail/PROJECT.md` is unmarked curated prose after the first write. Merge semantics: leave byte-for-byte intact. A correct second run does **not** regenerate it from `templates/project/PROJECT.md`.
- `AGENTS.md` already contains `<!-- methodrail:start -->` … `<!-- methodrail:end -->`. Second run updates only that owned block; the block already matches the template pointer → no byte change.
- No knowledge, control, or verify skill to refresh.
- No duplicate links or second integration.

**Would a sloppy second run rewrite curated prose?** Yes, if the operator treated PROJECT.md as a generated template dump. That would violate `references/merge-semantics.md` rule 1. Scored as pass for the procedure as specified; this is a real pressure point for v0.6 init (PROJECT.md is generated once, then curated with no ownership markers).

## Completion report

### Created

- `/tmp/express-methodrail-dogfood/.methodrail/PROJECT.md`
- `/tmp/express-methodrail-dogfood/AGENTS.md` (Methodrail pointer only)

### Updated

- None.

### Preserved

- All pre-existing Express files (Readme, lib, tests, examples, CI, History, license). No AI files existed to merge.

### Skipped

- `.methodrail/knowledge/*` — no non-obvious domain that docs/code do not already state.
- `.methodrail/control/CONTROL.md` — start/doctor/drive/stop are the package scripts and optional `node examples/<name>`.
- `create-verification-skill` / `.agents/skills/verify-express` — library; mocha+supertest is the drive path. Inventing a localhost:3000 recipe would be fake runtime infrastructure.
- `.cursor/rules/methodrail.mdc` and `CLAUDE.md` — would be a second integration and the wrong convention for this repo.
- Copying global Methodrail skills.
- Running tests or example servers.

### Refreshed

- None (first init).

### Unresolved / human decisions

- Express maintainers never chose AGENTS.md. The pointer is a Methodrail dogfood install, not an upstream convention. Do not PR this into expressjs/express without their request.
- `package.json` says 5.2.1 while `History.md` leads with Unreleased Changes. Version string is evidence from the manifest, not a claim that HEAD is a release tag.
- Tests were not executed in this environment; commands are transcribed, not proven green here.

## Main quality findings

1. **Library vs service is the load-bearing init judgment.** Express looks like an HTTP service (examples listen on 3000; Readme shows `app.listen`). Init correctly classified it as a library and documented static verification instead of generating `verify-express`. That matches `create-verification-skill` (“do not use on libraries with no runnable surface”) and `control-investigation.md` (skip CONTROL.md when answers are one script). If v0.6 init were scored only on “did we produce a verify skill?”, this run would look incomplete; against the checklist it is the right no-op.

2. **PROJECT.md stayed an index.** 74 lines / 5005 chars / 0 fences. It does not clone the Readme (no team list, philosophy, or generator tutorial) and does not dump `lib/` or `test/`. The highest-leverage facts are boundaries: published `files`, external `router`, `.npmrc` + no lockfile, tests vs `app.listen`.

3. **One thin integration, no skill copy.** AGENTS.md is three lines inside Methodrail markers. Zero global skills in the clone.

4. **Idempotency depends on merge discipline, not file shape.** PROJECT.md has no `methodrail:` markers. A second init that “fills the template again” would rewrite curated prose. The skill text forbids that; the file format does not mechanically prevent it.

5. **Command fidelity without execution.** Scripts match `package.json` and CI. Not running `npm test` was intentional (checklist item 16). A later dogfood could prove the transcribed `npm test` line still works on this Node, separately from init quality.

## Clone hygiene

Express changes live only under `/tmp/express-methodrail-dogfood`. They were not copied into the Methodrail working tree. Methodrail git should show only this report among init-dogfood artifacts.

To drop the clone: `rm -rf /tmp/express-methodrail-dogfood`.

## Generated `.methodrail/PROJECT.md` (copy)

```markdown
# Project

Pointer index for agents working in [expressjs/express](https://github.com/expressjs/express) (v5.2.1). Canonical product docs stay on [expressjs.com](https://expressjs.com/) and in [Readme.md](../Readme.md). This is a Node HTTP **library**, not a packaged service.

## Purpose

Express is a small CommonJS web framework: `express()` returns a request handler with routing, middleware, and HTTP helpers. Consumers build servers; this repo publishes the framework.

## Important boundaries

- Published npm surface is only `index.js` and `lib/` ([package.json `files`](../package.json)). `examples/` and `test/` are not shipped.
- Routing lives in the `router` package; body parsing in `body-parser`; static files in `serve-static`. Do not look for `lib/router.js`.
- Node `>= 18` ([engines](../package.json)). CI also runs a [legacy Node 16/17](../.github/workflows/legacy.yml) job; do not treat that as the supported floor.
- `.npmrc` sets `ignore-scripts=true`, `package-lock=false`, and `allow-git=none`. `package-lock.json` is gitignored: use `npm install`, not `npm ci`.
- Security reports go through the [security policy](https://github.com/expressjs/express/security/policy), not public issues.

## Canonical commands

Copied from [package.json](../package.json) and [CI](../.github/workflows/ci.yml). There is no build or typecheck script.

- Install: `npm install` (CI lint job: `npm install --ignore-scripts --include=dev`)
- Test: `npm test` (mocha + supertest; `--require test/support/env`)
- Lint: `npm run lint` / `npm run lint:fix`
- Coverage (local): `npm run test-cov`
- CI test: `npm run test-ci`
- Example server (optional demo only): `node examples/hello-world` listens on port 3000 when not required as a module

Do not invent `npm start`. The generator mentioned in the Readme is a separate package: [express-generator](https://github.com/expressjs/generator).

## Verification

This repository has no user-facing app to launch. Representative HTTP behavior is already driven in-process by mocha + [supertest](../test/app.js). `create-verification-skill` was skipped: a project-local verify skill would invent a long-running server the tests do not need.

- Library / `lib/` change: `npm test` (narrow with mocha grep against `test/<area>.js` when the change is local)
- Example change: corresponding file under [test/acceptance/](../test/acceptance)
- Lint-only / style: `npm run lint`
- Cross-cutting or release-risk: `npm run test-ci`
- Manual demo (not a proof gate): `node examples/<name>` per [examples/README.md](../examples/README.md)

`test/support/env.js` sets `NODE_ENV=test`. Evidence is mocha reporter output. No CONTROL.md: start/doctor/drive/stop collapse to the scripts above.

## Domain vocabulary

- **app**: function returned by `express()`; also an EventEmitter with `lib/application.js` methods
- **middleware**: `(req, res, next) => void`; mounted with `app.use`
- **Router / Route**: constructors re-exported from `router` ([lib/express.js](../lib/express.js))
- **req / res**: prototypes mixed onto Node's IncomingMessage / ServerResponse
- **view engine**: `app.engine` / `app.render` via [lib/view.js](../lib/view.js)
- **mountpath / parent**: sub-app mounting (`app.use('/blog', otherApp)`)

## Architecture pointers

- Entry: [index.js](../index.js) → [lib/express.js](../lib/express.js) (`createApplication`)
- App prototype: [lib/application.js](../lib/application.js) (`app.init`, settings, `app.listen` wraps `http.createServer`)
- Request/response: [lib/request.js](../lib/request.js), [lib/response.js](../lib/response.js)
- Docs: [expressjs.com](https://expressjs.com/), [v5 migration](https://expressjs.com/en/guide/migrating-5)
- Changelog: [History.md](../History.md)
- Contributing (org template): [CONTRIBUTING.md](https://github.com/expressjs/.github/blob/HEAD/CONTRIBUTING.md)

## Runtime and control

No non-obvious boot sequence. Examples listen only when `!module.parent` (see [examples/hello-world/index.js](../examples/hello-world/index.js)). Tests must not call `app.listen`; they pass the app to supertest. Do not generate CONTROL.md unless a future workflow needs coordinated multi-process setup (none observed).

## Important constraints

- Source is CommonJS (`'use strict'` + `require`). The Readme consumer snippet uses ESM `import`; that is the public API for Node consumers, not the package's own module format.
- Indent is 2-space; eslint forbids the global `Buffer` ([.eslintrc.yml](../.eslintrc.yml)).
- Default settings include `x-powered-by` on, `etag` weak, `query parser` simple, `trust proxy` false ([lib/application.js](../lib/application.js) `defaultConfiguration`).
- Breaking behavior for v4→v5 belongs in the [migration guide](https://expressjs.com/en/guide/migrating-5), not re-derived from v4 issues.

## Existing AI guidance

None found on first inspect (`AGENTS.md`, `CLAUDE.md`, `.cursor/`, `.claude/`, `.github/copilot-instructions.md` were absent). Methodrail pointer: [AGENTS.md](../AGENTS.md).
```
