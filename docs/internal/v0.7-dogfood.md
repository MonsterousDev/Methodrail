# v0.7 dogfood — Methodrail init and Reflect

Operator: Cursor Grok. Date: 2026-08-25. After the typed-note substrate and constructed specs were green (`npm run check`).

## Init

Ran the updated `/methodrail-init` obligations against this repository (inspect, refresh, do not recreate).

- Preserved [`.methodrail/PROJECT.md`](../../.methodrail/PROJECT.md) as a pointer index. No new sections, no cloned README.
- Recognized that this tree has **no** `.methodrail/knowledge/` notes (typed or legacy). That is valid.
- Did not create typed notes. Init is lazy; promotion is Reflect.
- Did not install a second host integration.
- A second unchanged pass would be a no-op: PROJECT.md was not rewritten.

Rejected init proposals:

| Proposal | Why rejected |
| --- | --- |
| Typed `architecture.md` summarizing the plugin vs harness split | Already owned by README and `references/project-harness.md` |
| Typed `eval.md` restating artifact-backed graders | Already owned by `evals/README.md` and v0.6.1 reports |
| Placeholder `domain.md` / `behavior.md` | No recurring undocumented uncertainty; stubs fail information ROI |

## Reflect

Mined the v0.6.1 eval-integrity work and the v0.7 substrate against [reflect](../../skills/reflect/SKILL.md).

Rejected knowledge candidates:

| Candidate | Why rejected |
| --- | --- |
| Graders must ignore `behaviors_observed` | Already encoded in `src/eval/grade-outcome.ts` and eval reports |
| T8 Task B must not leak retries | Eval-lesson already in T8 report; the new `knowledge-reuse` fixture is the enforcement |
| TypeScript is maintainer validation, not a consumer runtime | Already in README |
| Init must not create typed notes | Now in `methodrail-init` and the note contract |

Structural-enforcement candidates that already exist: note schema tests, fixture graders, family-invariant projections.

**No candidate earned promotion.** Zero typed notes were written. That is a successful dogfood result for a repository that already encodes its expensive knowledge in tests and canonical docs.
