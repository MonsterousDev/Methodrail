# v0.8 public-repo dogfood — locked targets

Internal. Operator-approved 2026-08-31. Seed notes in disposable clones are authorized. Do not push to these remotes. Do not copy clone files into Methodrail. Do not tag this as v0.8.0 or as C7 live extras.

Cloud workers pin Methodrail `feat/v0.8-knowledge-governance` after it is committed and pushed. Until then this file only records the repo lock.

## Remotes

| Remote | License | Why | Used by |
| --- | --- | --- | --- |
| [expressjs/express](https://github.com/expressjs/express) | MIT | Continuity with the v0.6 init dogfood. No `AGENTS.md` / `CLAUDE.md` / `.methodrail`. Real split: `lib/` vs `examples/`. Observed: `lib/response.js`, `examples/hello-world`. | workers 1, 3, 5 |
| [denoland/std](https://github.com/denoland/std) | MIT | Real product (not an AGENTS.md template repo). Root `AGENTS.md` (~4 KB). Package-per-directory (`http/`, `fs/`, `_tools/`). Shallow-clone sized (~26 MB git metadata). | worker 2 |
| [pallets/flask](https://github.com/pallets/flask) | BSD-3-Clause | Non-Node library so init cannot assume a Methodrail consumer runtime. `src/flask/` vs `examples/` vs `tests/`. `src/flask/json/provider.py` and `sessions.py` exist. | worker 4 |

Each worker gets its own `/tmp/methodrail-v08-dogfood-<slice>-*` clone (`git clone --depth 1`). Independent even when the remote is shared.

## Rejected

| Remote | Reason |
| --- | --- |
| vercel/ai, openai/codex, prisma/orm | Too large for a one-session interview |
| agentsmd/agents.md and AGENTS.md starter kits | Meta instruction repos; preservation would be circular |
| honojs/hono | Fine library, but no `examples/` split as clean as Express |
| koajs/koa, pallets/click | Smaller greenfield options; Express already covers Node init and has a prior scorecard |
| vitest-dev/vitest, Textualize/rich | Larger than needed |

## Worker assignment

| Worker | Slice | Clone | Task (do not add governance hints) |
| --- | --- | --- | --- |
| 0 | Maintainer verify | Methodrail freeze SHA only | `npm run check`. Local is fine. |
| 1 | Init, greenfield | express | `/methodrail-init`. In-repository `.methodrail/`. No typed notes. Second init no-op. Compare against [v0.6-init-dogfood.md](./v0.6-init-dogfood.md) without copying it. |
| 2 | Init, preserve AI files | denoland/std | Init. Existing `AGENTS.md` body stays. At most the Methodrail-owned pointer block. No second integration. |
| 3 | Seeded applicability | express (fresh clone) | After init, seed one verified note: include `lib`, exclude `examples`. Task: change `examples/hello-world` only. Must not apply a `lib/` invariant to the example. |
| 4 | Seeded dispute | flask | After init, seed two reciprocal disputed notes on `src/flask/sessions.py` (exclusive session mechanisms, no deciding evidence in the prompt). Task: implement a small session-related helper the prompt does not decide. No source patch; name both claims. |
| 5 | Seeded retirement | express (fresh clone) | After init, seed retired `app.del` plus successor `app.delete` (Express history already records that deprecation). Task: add a DELETE route under `examples/` without mentioning migration. Use `app.delete`; do not rewrite notes. |

Worker 0 is not a cloud clone. Workers 1–5 are cloud agents, `cloud_base_branch` = the pushed freeze branch. Load skills from that checkout, not a preinstalled plugin.

## Seed authorization

Seeds exist only in the clone. Label the note body `dogfood fixture, not upstream knowledge`. Index them in that clone’s PROJECT.md. After the seed, no further governance writes.

## Next

Commit and push the v0.8 branch (no `0.8.0` bump), then spawn workers 1–5 with standalone briefs. Parent aggregates. Write `docs/internal/v0.8-public-repo-dogfood.md` only after review. C7 constructed live battery stays a separate approval.
