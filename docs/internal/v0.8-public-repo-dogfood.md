# v0.8 public-repo dogfood

Internal maintainer record. Operator: Cursor parent plus local swarm workers. Date: 2026-08-31.
Methodrail freeze: `fc9323c` on `feat/v0.8-knowledge-governance`. Package version still **0.7.1**.
This is **dogfood**, not C7 live extras, not constructed specification `passed`, not empirical `helped`.
No git push to cloned remotes. Clones lived under `/tmp/methodrail-v08-dogfood-*`. Methodrail was not committed except this report if the operator chooses.

Cloud workers were requested. Cursor returned that the GitHub app is not installed on Methodrail, so isolated cloud VMs did not run. Slices 1–5 ran as local Task workers with `/tmp` clones.

## Results

| Slice | Target | Result | Notes |
| --- | --- | --- | --- |
| 0 maintainer gate | Methodrail `fc9323c` | **PASS** | `npm run check` (tsc, tests, validate, eval) |
| 1 init greenfield | expressjs/express@023767f | **PASS** | In-repo PROJECT.md + AGENTS.md pointer. No knowledge notes. Second init no-op. Source untouched. |
| 2 init preserve | denoland/std@ca58f94 | **PASS** | Existing AGENTS.md body intact; pointer block only. Second init not re-run. |
| 3 applicability | expressjs/express@023767f | **PASS** | Seeded note include `lib` exclude `examples`. Task path `examples/hello-world/index.js` → `not-applicable`. `lib/` clean. Note hash unchanged. |
| 4 dispute | pallets/flask@d318b68 | **PASS** | Reciprocal disputed notes `blocked-disputed`. No `src/flask` patch. Both mechanisms named. |
| 5 retirement | expressjs/express@023767f | **PASS** | Successor `app.delete` used. Retired `app.del` unused. Knowledge bytes unchanged after task. Hello-world acceptance: 3 passing. |

## Issues (one-liners)

- Cloud isolation **BLOCKED** (Cursor GitHub app missing on the repository).
- Slice 2 did not re-execute a second init; idempotency inferred from a 4-line pointer diff.
- Slice 4 used `NullSessionInterface` as the dogfood claim name; Flask source has `NullSession`, not that class.
- Most clones did not run full upstream test/CI. Slice 5 ran focused mocha acceptance only.
- Linked-external placement was not exercised.

## Gaps

- C7 constructed live battery not run.
- Windows not tested.
- Seed notes are disposable fixtures, not upstream knowledge. Do not PR them.
- Freshness after example edits became `review-required`; notes were left unchanged by design.

## Conclusion

On these three public clones, init stayed a pointer harness, existing AGENTS.md was preserved, a scoped note was not applied to an excluded path, a dispute did not pick a winner, and a retired claim was not used. That is qualitative dogfood on `fc9323c`. It does not by itself justify tagging `0.8.0`.
